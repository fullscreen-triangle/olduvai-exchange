import RailPage from "@/components/RailPage";

/**
 * What is forecast, what it rests on, and what it is not.
 *
 * ⚠️ The one page here whose gate may never lift. It waits on a decision that an honest
 * forecast is possible at single-holding resolution at all — not on work. A number shown here
 * would be read as evidence, and an unrecomputable number that is read as evidence is worse
 * than no number.
 */
export default function Page() {
  return <RailPage />;
}
